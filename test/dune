; Phase 0 scaffold test: proves the toolchain wiring end to end on the ox switch
;  - risc5        : our (placeholder) Hardcaml design library
;  - oracle       : the OCaml emulator core, compiled from the vendored submodule
;  - hardcaml     : build + simulate a circuit
;  - hardcaml_waveterm : render an ASCII waveform of the counter smoke

(tests
 (names
  test_scaffold
  test_fp_adder
  test_fp_multiplier
  test_fp_divider
  test_cpu_lockstep)
 (libraries risc5 oracle hardcaml hardcaml_waveterm)
 ; Phase 3b: the frozen FP vectors (vendored, data-only) feed test_fp_adder's replay.
 (deps ../vendor/oberon-risc-emu-ocaml/test/data/fp_vectors.txt)
 (preprocess
  (pps ppx_hardcaml)))
