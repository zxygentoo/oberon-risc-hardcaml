(** Oberon RISC5 — Hardcaml design library.

    Placeholder for Phase 0 scaffolding. The real hardware modules (barrel shifters, ALU +
    flags, iterative MUL/DIV, the FP units, the register file, the CPU core, and the SoC)
    are added one at a time from Phase 1 onward, each verified in lockstep against the
    [risc_core] oracle. *)

let phase = "0 — scaffolding"
